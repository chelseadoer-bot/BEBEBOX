# 베베박스 환경 설정 (이 파일을 .env 로 복사한 뒤 값을 채워 주세요)
# cp .env.example .env   (Mac/Linux)
# copy .env.example .env (Windows)

# Google Gemini API KEY (사진 퀘스트 AI 분석용)
# https://aistudio.google.com/apikey 에서 발급
GEMINI_API_KEY=

# 서버 포트 (기본 8080)
PORT=8080
